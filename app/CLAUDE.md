# CLAUDE.md — Connect! (app/)

Implementation memory for the **runnable app** in `app/`. Read this before editing.
Product/business rules and the Supabase target schema live in the spec-level
`../Connect! UI Lab/CLAUDE.md` (referred to here as **spec §x**); this file documents
what's *actually built in code*. When code and a doc disagree, trust the code and flag it.

---

## 1. What the app is

- **Connect!** — a mobile-first pickup-sports matchmaking app for **Doha, Qatar** (currency **QAR**).
- Sports: **padel · tennis · badminton · running**.
- Core user jobs: **discover** open matches → **host** a match → **join / request / approve** players (+ FIFO **waitlist**) → **chat** (per-match group + DMs) → **record results** post-match.
- Stage 1 scope: free matching only, cash-on-arrival (no in-app payments), 18+, Doha-only. See spec §1.

---

## 2. Tech & file structure

**Stack** — React 19 + TypeScript, **Vite 8**, **Tailwind v4** (`@tailwindcss/vite`, config-less — tokens live in `src/index.css`), `react-router-dom` v7, **lucide-react** icons, `clsx`/`tailwind-merge`/`cva` (via `cn()` in `lib/utils.ts`). No shadcn; components are local. Run: `npm run dev` (Vite), `npm run build` (`tsc -b && vite build`), `npm run lint`.

**Entry / frame** — `index.html` (loads Inter + Instrument Serif + IBM Plex Sans Arabic) → `main.tsx` → `App.tsx` (`<Routes>`). Every screen renders inside `Shell` = the phone frame: `max-w-[430px]`, `h-dvh`, cream page on a dark backdrop, ambient `Blobs`, optional floating `BottomNav`.

**State** — single **in-memory reactive store** (`lib/store.ts`) via `useSyncExternalStore`, seeded from `lib/mock/*`. Repository-shaped accessors mirror future Supabase queries. A few things persist to `localStorage`: `connect.freshAccount`, profile-done flag, and **`connect:hostedMatch`** (see §5). ES modules throughout — **no `window` globals, no inline Babel** (that was the old prototype in `../Connect! UI Lab/`).

**Key files**

| Area | File(s) | Owns |
|---|---|---|
| Design tokens | `src/index.css` | CSS vars on `:root`, Tailwind `@theme` mapping, type-scale `@utility`s, `.num`/`.screen`/`.scroll`/`.hscroll` |
| Store | `lib/store.ts` | `DB`, `useDB()`, accessors (`hostedMatches`, `joinedMatches`, `discoverFeed`, `waitlistPosition`…), `actions.*` (CRUD + join/approve/invite/waitlist/chat) |
| Status logic | `lib/status.ts` | `computeStatus` (read-time lifecycle), `canCancelCleanly` (≥2h), `noShowReportThreshold` |
| Formatting | `lib/format.ts` | `hm`/`timeRange` (24h), **`to24h`** (normalise `"HH:mm"` → zero-padded 24h), `dayLabel`, `matchKind`, **`courtLabel`/`courtNumberLabel`** (render a court value as "Court N" — only prefixes "Court " when the stored value is a bare number, so seed values like "Court 4"/"Hall 2"/"Court A" pass through unchanged), `skillLabel`, `initials`, `hoursUntil`, **`countdownUntil`** (Home "Next up": `"3d"`/`"5h"`/`"soon"`)… |
| Date/time (form) | `lib/datetime.ts` | shared create/edit-form helpers: `keyOf`, `labelFromKey` ("Thu · May 23"), `addMinutes`/`diffMinutes`, `normalizeTime` (parse free-typed time) |
| Hosted match (demo SoT) | `lib/hostedMatch.ts` | `HostedMatch` type, `read/write/clearHostedMatch`, reactive `useHostedMatch()` (localStorage `connect:hostedMatch`) |
| Types | `lib/types.ts` | `Match`, `User` (incl. `bio`/`area`/`city`/`languages`/`verified`), `MatchRequest`, `Venue`, `Sport`, `SkillLevel`, statuses |
| Sport glyph | `lib/sports.tsx` | **`SPORT_EMOJI`/`sportEmoji(sport)`** — single source for the per-sport emoji (padel 🏓 · tennis 🎾 · badminton 🏸 · running 🏃), shown in front of the label on create/edit forms + profile |
| Profile sports | `lib/profile.ts` + `screens/profile/SportsLevelSection.tsx` | multi-sport list: `read/write/clearProfileSports` (localStorage `connect.profileSports`), `SkillRating` ⇄ `SkillLevel` mappers; section lets a player add several sports + self-rate each |
| Locations | `lib/locations.ts` | `QATAR_CITIES`, `AREAS_BY_CITY`, `areasFor(city)` — reference data for the Edit Profile city/area selects |
| Mock data | `lib/mock/data.ts`, `lib/mock/venues.ts` | seeded users/matches/threads; `VENUES` (~27 Doha venues) + `venuesForSport` |
| Shared atoms | `components/controls.tsx` | `Segmented`, `Toggle`, `Slider`, `DualSlider`, `PlayerDots`, `CTA`, `MiniMap` |
| Shared UI | `components/` | `Shell`, `Eyebrow`, `MatchCard` (THE canonical card), `SportArt`, `Avatar`, `Blobs`, `BottomNav`, `Toast`, `lifecycle.tsx` |
| Create/Edit (store) | `screens/matches/CreateMatchScreen.tsx` | `CreateMatchScreen({mode})` — store-backed form at `/matches/create` & `/matches/:id/edit` |
| Create/Edit (demo) | `screens/matches/EditMatchScreen.tsx` | `EditMatchScreen({mode})` — **localStorage** form at `/matches/edit-demo` & `/matches/create-demo` (see §5) |
| When section (shared) | `screens/matches/WhenCard.tsx` | `WhenCard` — date strip + native calendar + typeable 24h start/end + duration; used by **both** create/edit forms so date/time never drift. `TimeField` is **two cells (HH / MM) with a fixed `:` between**, each digit-only, committing a `normalizeTime`d value on blur/Enter |
| Venue picker | `screens/matches/VenuePicker.tsx` | bottom sheet; `VenueSelection`; court vs running-route |
| Home | `screens/home/` | `HomeScreen`, `FirstTimerHome` (empty state), `HostedMatchCard`, `WeekMatchCard`, `RecordResultHomeCard` |
| Other screens | `screens/{discover,chat,profile,postmatch,safety,auth}/` | Discover, chat list/threads/DM, profile/edit/settings, post-match recording, safety, auth+onboarding |

Routes are in `App.tsx`; 3 tabs (Discover · Home · Chat), Home is default; My Matches lives under Home (spec §4).

---

## 3. Design system / tokens — "Editorial Calm"

Source of truth = `src/index.css` (`:root` → Tailwind `@theme`). **Never hardcode a hex/size — consume the token.**

| Token | Value | Use |
|---|---|---|
| `--surface-page` | `#F4F0E8` | cream app background |
| `--surface-card` | `#ffffff` | cards |
| `--color-text` (`ink`) | `#1a1a1a` | primary ink |
| `--color-text-muted` | `rgba(26,26,26,.55)` | labels / secondary |
| `--color-text-faint` | `rgba(26,26,26,.42)` | disabled |
| hairline border | `rgba(26,26,26,.10)` | card / divider lines |
| `--color-brand` | `#C76A48` | terracotta accent — CTAs, selected, italic emphasis, slider fill |
| `--color-brandstrong` | `#A04A2C` | pressed brand |
| `--color-accent` | `#8A6B3A` | eyebrows / identity / links |
| `--color-text-onbrand` | `#F4F0E8` | cream text on brand/dark |
| lifecycle | success `#3E7A5A` · info `#3C6079` · warning `#8A6B3A` · danger `#A23B2C` · live `#C25A38` · neutral `#8C8478` | match states |

- **Type — two families only.** `--font-ui` = **Inter** (400/500/600, body + labels); `--font-display` = **Instrument Serif** (H1, time numerals, big numeric values, input headlines). Italic display tinted `text-brand` = emphasis. Arabic falls back to IBM Plex Sans Arabic. Scale utilities: `type-display-xl/l/s`, `type-title`, `type-body(-lg)`, `type-label`, `type-caption`, `type-overline`.
- **Card spec:** radius **22px**, **1px hairline** border, padding **~18px**, optional `shadow-card`.
- **Eyebrow:** accent dot + 10.5px uppercase 0.2em muted label (`<Eyebrow accent=… />`).
- **Controls** (`controls.tsx`): `Segmented` (selected = brand fill / cream text), `Slider`/`DualSlider` (brand fill + ticks), `Toggle` (iOS switch), `PlayerDots` (first = brand w/ initials, rest dashed `+`), `CTA` (brand pill, `disabled`/`loading`/`big`).
- **`accent` is a single threaded prop** (defaults to a token) on `Eyebrow`, `SportArt`, `Avatar`, etc. — pass it, don't restyle.
- **Sport glyph = emoji, not icon.** Each sport renders a shared emoji in front of its label via `sportEmoji()` (`lib/sports.tsx`) — 🏓 padel · 🎾 tennis · 🏸 badminton · 🏃 running. Used on the create/edit sport pills and the profile Sports & level rows. Don't reintroduce per-sport lucide icons; add new sports to the one `SPORT_EMOJI` map.
- **Numerals:** Western digits always; functional figures use `.nums-tabular`; times/numbers stay LTR via `.ltr-nums` (RTL-safe).

---

## 4. Data model

There are **two shapes**: the canonical store `Match` (Supabase-shaped, snake_case) and the **edit-form / `HostedMatch`** shape (camelCase, what the create/edit form + Home card use). Keep them in sync via the mapper in `EditMatchScreen.save()`.

**Store `Match`** (`lib/types.ts`) — `id, host_id, sport, name?, venue_id?, venue_name, venue_location?, court_number?, route_start?, route_end?, round_trip, start_time(ISO), end_time(ISO), skill_level, total_spots, spots_available, fee_total?, fee_per_player?, join_mode('open'|'approval'|'invite'), status('active'|'cancelled'), notes?, created_at`. Read-time `MatchStatus` is computed, not stored (§5). **`name`** is the optional host-given title from the create/edit form: persisted live via the `matches.name` column (`create_match`'s `p_name` + `updateMatch` cols + `repo.mapMatch`), and rendered as the **`MatchCard` title** when present (else the card falls back to the derived `matchKind at courtLabel`, e.g. "Doubles at 4"). So every `MatchCard` surface (Home "You're hosting", Discover, All Matches) shows the typed name; venue still shows on the card art + the MapPin line.

**`HostedMatch`** (`lib/hostedMatch.ts`) — the host's own match, the form's working shape:

| Field | Meaning |
|---|---|
| `sport` | padel / tennis / badminton / running |
| `name` | match title (serif headline) |
| `dateKey` / `dateLabel` | ISO `yyyy-mm-dd` / display `"Thu · May 23"` (derived from `dateKey` via `labelFromKey`, any month) |
| `startTime` / `endTime` | **24h `"HH:mm"`**, displayed as-is (no AM/PM anywhere — see §5) |
| `matchType` | `casual` \| `competition` |
| `gender` | `mixed` \| `ladies` |
| `requireApproval` | join instantly vs approve (maps to `join_mode`) |
| `isFree` / `pricePerPlayer` | Free vs split; QAR per player (digits string — typing-only `type="text"` inputMode numeric, §5) |
| `players` / `filled` | **total** spots / taken (host = 1). The create/edit form's slider chooses **players needed** (others, range 1–7); `total_spots = needed + 1` (host occupies the extra spot), so a card reads "{needed} spots left" right after create (§5) |
| `minLevel` / `maxLevel` | 1–5 skill range (see `LEVEL_NAMES`) |
| `waitlistOpen` / `waitlistSize` | queue toggle + 1–8 cap |
| `description` | free text (≤240) |
| `venueName, area, setting, court` | court-sport location |
| `isRoute, routeEnd, loop, km` | running route instead of a court |

- **`LEVEL_NAMES`** (edit form, 5-step): `Baby · Beginner · Low int. · High int. · Advanced`. (Distinct from the store's 7-step `SkillLevel`; the form maps the 1–5 range onto `skill_level` in `CreateMatchScreen` via `levelRange`.)
- **Venues:** `VENUES` seed (~27 curated Doha venues, `venuesForSport(sport)`). `VenueSelection` (`VenuePicker`) carries `id|'custom'|'route'`, name/area/setting, optional **court # (separate free-text)**, or route `endName/loop/km`. Running has no fixed venues → start/finish route. (Note: the brief's `locDist` is **not** modelled.)

---

## 5. Core logic & conventions

- **Create vs Edit (`mode` prop).** Both `CreateMatchScreen` and `EditMatchScreen` take `mode: 'create'|'edit'`.
  - Edit pre-fills from the existing match; footer = **Save changes** + **Cancel match** (destructive confirm). Create opens blank; footer primary = **Create match**.
  - `CreateMatchScreen` (store flow) enforces a **required-choice guard**: Cost *and* Joining must be explicitly chosen before create (reminder sheet otherwise). In edit they're already set.
  - `EditMatchScreen` (demo flow) branches on `isCreate`: create shows the "New match · {date}" eyebrow + "Host a match." hero and hides the destructive Cancel link; edit shows "You're hosting" + "Edit your match." + Cancel.
- **Persistence / source of truth.** The **demo** create/edit flow (`EditMatchScreen`) is the single source of truth for *the host's own match*: it writes **`localStorage` `connect:hostedMatch`** via `writeHostedMatch`, and `HomeScreen` reads it with the reactive `useHostedMatch()` — so Home's "You're hosting" card reflects creates/edits instantly and **survives reload**. Save → `/home`; Cancel → `clearHostedMatch()` (card disappears). When present it is **preferred over** the seeded store hosted matches. The **store** flow (`CreateMatchScreen` + `actions.createMatch/updateMatch`) is the older, separate path — see §7.
- **Fresh account = clean slate.** `actions.startFreshAccount()` (the onboarding "Create account" step) resets the seeded data via `freshDB()` **and** calls `clearHostedMatch()` — otherwise a `connect:hostedMatch` left in localStorage from a prior session would make `HomeScreen` render the "You're hosting" variant instead of `FirstTimerHome`. So a brand-new account always lands on the first-timer Home (seeded Discover feed + Find a match / Host one).
- **Onboarding identity → every screen.** Sign-up answers live in the `onboarding` proxy (`lib/onboarding.ts`, localStorage `connect.onboarding`): **`name`** is written on the Sign Up screen's *Create account* (after `resetOnboarding()`; empty falls back gracefully), **`sport`** and **`skill`** by the questionnaire steps. The single source for the *current user's display identity* is the store user `getUser(db, currentUserId)` — **no screen reads `useAuth().user` directly**. For a **fresh account** (`isFreshAccount()`), `withSignedIn` overlays the onboarding `name`/`sport`/`skill_level` onto that user, so the sign-up answers win over the dev/mock signed-in profile. This matters because `AuthProvider` re-applies the dev `mockUser` ("Jen") via `setSignedInUser` on every mount/reload; without the overlay it would clobber the typed name. Result: the typed name **and the avatar initials derived from it** are consistent on every page (Home greeting/avatar, Profile, Edit Profile, Chat, "Hosted by", `MatchCard`/`AvatarStack`, post-match) and survive reload. `freshDB()` applies the same overlay for the module-init case (before `AuthProvider` mounts). The `mockUser` "Jen / padel / intermediate" only shows in the seeded/demo (non-fresh) account.
- **First-timer Home feed = Discover, filtered.** `FirstTimerHome` renders the first 3 of the same `discoverFeed(db)` Discover uses (no separate array), filtered by the user's picks: `m.sport === sport && (m.skill_level === skill || m.skill_level === 'any')`. If that's empty it falls back to the full nearby feed with an honest label (`No {Sport} at your level — nearby`) — never blank (§5). Ids stay stable across Home/Discover. **The Find a match / Host one buttons + this seeded rail live ONLY on `FirstTimerHome`** (the no-joined/no-created empty state); the returning-user `HomeScreen` deliberately omits them and shows Next up · You're hosting · This week instead. **Host entry points are mode-aware via the single `HOST_CREATE_ROUTE` const (`lib/hostedMatch.ts`)** — every "host a match" button (FirstTimerHome "Host one", Discover header **+** and footer nudge) routes through it: **live** → the **store** flow `/matches/create` (`actions.createMatch` RPC → persists → surfaces under "You're hosting" via `hostedMatches(db)`); **mock** → the demo flow `/matches/create-demo` (writes `connect:hostedMatch`, read by `useHostedMatch()`). This is required because live Home **ignores** `connect:hostedMatch` (browser-global key, not per-user → cross-account bleed), so a demo-created match would never show for a live user. Both flows route to `/home` on create.
- **Time handling — 24h everywhere, no AM/PM.** Times are stored and displayed as **24h `"HH:mm"`**; there is **no 12h/AM-PM anywhere** in the UI. `format.to24h()` normalises an `"HH:mm"` string to zero-padded 24h for display; `MatchCard`/details use the existing 24h `hm`/`timeRange`. Start/End on **both** create/edit forms are **typeable** — the shared `WhenCard.TimeField` renders **two digit-only cells (HH / MM) with a fixed `:` between them** (no clock glyph); each cell accepts digits and the pair commits a value normalised by `datetime.normalizeTime` on blur/Enter (clamped to 0–23 : 0–59, falling back to the prior value on garbage).
- **Date selection.** `WhenCard` shows a 10-day quick-pick strip **anchored to today** plus a native `<input type="date">` calendar pill for any date; picking a date outside the window re-anchors the strip so the selection stays visible. `dateLabel` is derived from `dateKey` via `datetime.labelFromKey` (correct for any month). Create flows pass `restrictPast` so the calendar's `min` is today.
- **Numbers-only inputs.** Price uses `type="number" inputMode="numeric"`, `onChange` sanitises to `/[^0-9]/g`, keydown blocks `e/E/+/-/.`, and the **`.num`** class hides native spinners.
- **Scroll model.** One scroll owner per screen: `Shell`/`.screen` clips with `overflow:hidden`; an inner `.scroll` owns vertical scroll (`56px 22px 130px` padding for back-button + sticky footer). Horizontal rails (date strip, sport pills) use **`.hscroll`** (hidden scrollbar). Never `scrollIntoView`.
- **Duration logic** (in `WhenCard`, shared by both forms). Picking a duration preset (`30/60/90/120` or custom popover) rolls **End = Start + duration**; changing Start re-rolls End by the current duration (`datetime.addMinutes`/`diffMinutes`).
- **Spots = "players needed", host occupies one.** The create/edit form's Players slider chooses how many **others** the host needs (range **1–7**, labelled "Players needed · Not counting you"); `total_spots = needed + 1`, `spots_available = needed` at create (RPC/mock both seat the host). So the card reads "{needed} spots left" immediately. `matchKind` still derives Singles (total 2) / Doubles (total 4) from `total_spots`, so the 2–8 total range is unchanged. **When editing the player count**, `CreateMatchScreen.save()` recomputes `spots_available = max(newTotal − alreadyJoined, 0)` (joined = `existing.total_spots − existing.spots_available`) so the count can't drift; `updateMatch`'s editable cols include `spots_available`.
- **Price input is typing-only** — `type="text"` + `inputMode="numeric"`, `onChange` sanitises to `/[^0-9]/g`, `.num` class (no spinners). Don't use `type="number"` for price (it shows steppers).
- **`MatchCard` always shows the host.** Pass `host` to the card (incl. the Home "You're hosting" card → `host={getUser(db, m.host_id)}`) so the "Hosted by …" line renders. The avatar stack builds a `roster` that **prepends the host if absent from `players`**, so the host monogram heads the stack even before `match_players` hydrates.
- **Lifecycle / status** (`computeStatus`, read-time, no cron): `open` (spots left) → `full` → `live` (start≤now<end) → `completed` (≤24h after end) → `closed`; `cancelled` is stored. Cancel cleanly only **≥2h** before start (`canCancelCleanly`), else no-show. **`actions.cancelMatch` optimistically mutates the store in BOTH modes** (sets `status:'cancelled'`, expires waitlist) then, in live mode, `afterWrite`s the Supabase update + rehydrates — so Home (which navigates immediately after cancel) drops the card on the next render instead of waiting for the round-trip. **Cancelling also removes the match's group chat from the Chat page:** `inboxThreads` filters out any thread whose match is `cancelled`, so the conversation disappears from the inbox for every participant on the next render (live: the status flip propagates via Realtime). The thread row is **not** deleted — the read-only `MatchChatScreen` ("This match was cancelled — chat is read-only") stays reachable by deep-link as a graceful fallback so history isn't lost.
- **Waitlist** (`actions.joinWaitlist/leaveWaitlist`, `waitlistPosition`): full matches only, FIFO by `created_at`, **auto-promote** the earliest waitlister when a slot frees — **no host approval on promotion** even for `approval` matches. See spec §5 for the full rules (threshold table in `noShowReportThreshold`).

---

## 6. Conventions for future edits

- **Tokens over hardcoded values**, always (`var(--color-…)` / Tailwind token classes).
- **Reuse the canonical `MatchCard`** — don't make per-page card variants. Reuse `controls.tsx` atoms instead of restyling.
- **Thread `accent` as a prop**; don't hardcode the terracotta.
- **Keep field names consistent across screens** (store `Match` ↔ `HostedMatch`); if you add a field, update the mapper in `EditMatchScreen.save()` and the `HostedMatchCard` render together so they can't drift.
- **Date/time UI goes through `WhenCard` + `lib/datetime`** — both create/edit forms render the same `WhenCard`; don't reintroduce a per-form date strip or time input, keep the two-cell HH `:` MM layout, and never re-add a 12h/AM-PM formatter.
- **Spots are entered as "players needed" (host excluded); store `total_spots = needed + 1`.** Don't relabel the slider back to total. Recompute `spots_available` whenever `total_spots` changes on edit. Price fields are typing-only (`type="text"` + numeric sanitise), never `type="number"`. Always pass `host` to `MatchCard`.
- **Match the cream / serif "Editorial Calm" look**; display moments in Instrument Serif, body in Inter, italic-brand for emphasis words.
- **Keep every screen inside `Shell`** (the phone frame) with the one-scroll model.
- **Prefer read-time computation** (`computeStatus`) over stored/derived flags; keep Stage-1 scope tight (spec §1) — confirm before building deferred features.
- **Profile/own-user page headers show only the Edit (pencil) action — no Settings gear** (Settings has its own `/settings` route). This is a standing rule for future pages too; if a page genuinely needs Settings in its header, confirm first.
- **Settings is reached via the top-right Home avatar, and is the back target for the own profile.** Both Home variants' corner avatars (`HomeScreen` + `FirstTimerHome`) link to `/settings`. The `<` back button on the **own** View Profile (`ProfileScreen own`) and on **Edit Profile** routes to `/settings` (not `navigate(-1)`); **Settings' own `<` routes to `/home`** (also not `navigate(-1)` — after a save→`/profile`→`/settings` hop, `navigate(-1)` would bounce back to the profile page instead of Home). So the full loop is avatar → Settings → View/Edit Profile → (save→View Profile) → back → Settings → back → Home. **Another player's** profile (`/players/:id`) keeps `navigate(-1)` so it returns to wherever it was opened from (roster/chat).
- **Profile identity reads the one source.** No screen reads `useAuth().user` directly — the current user's display identity is `getUser(db, currentUserId)`; persist edits through `actions.updateProfile` (never a per-screen copy). Profile name highlights the **first** name in italic accent (not the surname).
- RTL-aware: directional icons use `rtl:rotate-180`; numbers/times stay LTR (`.ltr-nums`).

---

## 7. Known gaps / TODOs

- **Two parallel create/edit flows.** `CreateMatchScreen` (store-backed, `/matches/create`, `/matches/:id/edit`) vs `EditMatchScreen` (localStorage demo, `/matches/edit-demo`, `/matches/create-demo`). They now **share the `WhenCard` date/time UI** (and `lib/datetime` helpers) so date + time can't drift, but still differ in persistence (store vs localStorage) and field set (the demo adds match name, gender, waitlist). **ALL host-create entry points route via the single `HOST_CREATE_ROUTE` const** (`lib/hostedMatch.ts`): **live → store flow** (`/matches/create`, persists to Supabase so it shows under "You're hosting"), **mock → demo flow** (`/matches/create-demo`, localStorage). Wired into `FirstTimerHome` ("Host one"), `DiscoverScreen` (header **+** and footer nudge), the Home header **+** (`HomeScreen`), and the My-Matches/All header **+** (`AllMatchesScreen`) — no hardcoded `/matches/create` left, so routing is single-sourced and identical for every account (seeded/dummy users and future signups). Edit routing mirrors this: the live/store "You're hosting" `MatchCard` edits via `/matches/:id/edit` (store flow); the mock `HostedMatchCard` edits via `/matches/edit-demo` (demo flow). These should eventually converge on one implementation/schema. **Note:** the demo flow is localStorage-only — in live mode it does NOT persist to Supabase and live Home ignores `connect:hostedMatch`, which is exactly why a demo-created match never appeared under "You're hosting" (fixed by `HOST_CREATE_ROUTE`).
- **Live Supabase path is now wired (dual-mode store).** `lib/store.ts` runs in one of two modes. **Live mode** activates when `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` are set (`.env.local`, gitignored — **currently present**, so the running app is live): on a real session `AuthContext` calls `connectLive(userId)`, which hydrates the whole `db` via `fetchSnapshot` (`lib/repo.ts`), subscribes a `connect-db` Realtime channel to **all** `public` postgres_changes → debounced `scheduleRehydrate`, and flips `liveReady = true`. With `liveReady`, each `actions.*` mutates the store optimistically **and** writes to Supabase — simple field writes via `afterWrite(supabase.from(...).update/insert(...))` (e.g. `updateProfile` → `users`), multi-row/atomic ops via `rpc('join_match' | 'approve_request' | 'accept_invite' | …)`. `disconnectLive()` tears the channel down on sign-out. **Mock mode** (no env / no session) is the fallback: in-memory `lib/mock/*`, reset on reload except the localStorage keys, with the dev-`mockUser` overlay. **Auth:** `lib/supabase.ts` shared client + `context/AuthContext.tsx` (`getSession` + `onAuthStateChange`, `signInWithPassword`/`signUp`/`signOut`), `useAuth()` → `{ user, session, loading, signIn, signUp, signInWithMock, signOut }` (`signUp` → `supabase.auth.signUp` returning `{ error, session, needsConfirmation }`; `signOut` returns `{ error }` and clears local state **only on success** — no half-sign-out). `AuthContext.toAppUser` maps the auth user → app `User`, seeding trust signals from `mockUser` until a `profiles`/`users` fetch covers them (the snapshot's `users` row now carries `bio`/`area`/`city`/`languages`/`verified`).
- **Sign-up is live-wired (real `supabase.auth.signUp`).** The real call runs on the **final "Creating account" step** (`CreatingAccountScreen` in `screens/auth/AuthScreens.tsx`), NOT on the Sign Up screen — it must pass the **gender** answer (Q2) in `options.data:{name,gender}` so the `handle_new_user` trigger seeds real gender (not the `'male'` stub). Email + password are captured on the Sign Up screen and carried across the questionnaire via new `onboarding.email`/`onboarding.password` draft fields (password cleared on success). On a returned session it writes the rest with an authenticated `users.update({dob,sport,skill_level,gender}).eq('id', session.user.id)` (RLS `id = auth.uid()` passes), then calls `connectLive(session.user.id)` again to re-hydrate from a **post-write** snapshot so Profile/Home reflect the captured sport/skill immediately (avoids a race where the auto-`connectLive` snapshots before the write commits). signUp **error** → error screen w/ "Back to sign up"; **no session** (email-confirmation ON) → "Check your email" screen (never silently routes to `/home`). **Config:** for Stage 1 testing, email-confirmation must be **OFF** in the Supabase dashboard (Authentication → Email → "Confirm email") so signUp returns a session; if ON, the flow correctly stops at "Check your email" and the profile fields aren't written (a row created in that window keeps null sport/skill — fix via Edit Profile). Mock mode keeps the old `startFreshAccount` demo path.
- **Every auth user MUST have a `public.users` profile row.** The whole app reads identity via `getUser(db, currentUserId)` from the snapshot's `users` table (not `useAuth().user`), and FK constraints bind `matches.host_id`/`match_players.player_id` to `users`. A signed-in auth user with **no** `users` row is a broken state — Home now degrades gracefully to `FirstTimerHome` (it won't blank), but Profile/hosting still need the row. The row is created by the **`handle_new_user`** auth trigger on signup; harden it to always set the NOT-NULL `gender` so it can't fail-and-orphan: `insert into public.users(id,name,email,gender) values(new.id, coalesce(meta->>'name', split_part(email,'@',1)), email, coalesce(meta->>'gender','male')) on conflict (id) do nothing`. To find orphans / inspect the live trigger: `select id,email from auth.users au where not exists (select 1 from public.users pu where pu.id=au.id);` and `select pg_get_functiondef('public.handle_new_user'::regproc);`. Backfill with the insert in the changelog.
- **Live DB needs seeding for a populated Discover / first-timer Home.** In live mode `discoverFeed`/`FirstTimerHome` read only Supabase, so a fresh project shows a **blank** feed (the `lib/mock/*` seed is mock-mode only). The live seed is **`Connect! UI Lab/supabase/seed.sql`** — idempotent (`on conflict do nothing` / `where not exists`), 6 dummy auth users (password `Test1234!`), venues, and matches across **all four sports** with `now()+interval` (always-future) start times so they compute `open`/`full` and surface in Discover + Home. Run it in the **Supabase dashboard → SQL Editor** (runs as `postgres`, bypassing RLS — the anon key in `.env.local` can't insert other users' rows). Re-run anytime to top up. Matches are hosted by the dummy users (not you), so they appear in `discoverFeed` (`host_id !== me`).
- **No payments** — every QAR figure (`fee_*`, `pricePerPlayer`) is informational; players pay the host directly.
- **No i18n/RTL content pass** beyond `i18n/en.ts` + utilities; Arabic strings/QA pending.
- **`/lab` route + `screens/Lab.tsx`** are a dev-only component lab; remove before launch.
- `HostedMatch` omits `locDist`/distance; `PlayerDots` initials and several card avatars are placeholder data.
- **Multi-sport list is localStorage-only (not in Supabase yet).** Edit Profile's Sports & level section (`SportsLevelSection`) lets a player add several sports + self-rate each; the full list persists via `writeProfileSports` → localStorage `connect.profileSports` (`lib/profile.ts`), and the editor + own ProfileScreen rehydrate from `readProfileSports`. Only the **primary** (first) sport + level is written onto the `users` row (`updateProfile`) and is therefore what propagates to other players / cards / Realtime. Promote the list to a `player_sports` table when multi-sport needs to be public (TODO in `lib/profile.ts`). `EditProfileScreen.save()` is **no longer a no-op** — it persists name/bio/area/city + primary sport/skill via `actions.updateProfile`, saves the full list, flags `completeProfile()`, then save-then-routes to **`/profile`** (§4).

---

_Last updated: 2026-06-14 — **leave match + leave chat line · chat unread = received-only · Alerts badge clears on open.** Three user-requested chat/match-flow changes. (1) **"Leave match" button on Match Details.** New sticky footer in `MatchDetailsScreen` (player view only) shown when `!hostView && joined && (status open|full)`: an outlined danger pill (`LogOut` icon, `--color-danger`) that calls `actions.leaveMatch(m.id)` → toast → `/home`. Hosts don't get it (they cancel from Edit). The body already reserved `paddingBottom:170` in player view, so it slots in without overlap. File: `screens/matches/MatchDetailsScreen.tsx`. (2) **"{Name} left the match" system line in the match group chat** (mirrors the "{Name} joined" line). **Mock:** `actions.leaveMatch` now drops the leaver from the thread's `participant_ids` and posts a `warm`/`userMinus` system line **before** `promoteFromWaitlist` runs (so a freed-then-promoted slot reads "X left the match" + "Y joined from the waitlist"). **Live:** the `cancel_participation` RPC already deletes the `match_players` row, firing the existing `trg_thread_leave` trigger — its function `_sync_thread_on_leave` (was: only `array_remove` from participants, no message) was recreated to also `insert` the system line, skipping the host (mirrors `_sync_thread_on_join`'s host guard). New migration `20260614000003_connect_leave_system_line.sql` — **applied live + verified** (`tone`/`icon` are free `text`, no CHECK, so `warm`/`userMinus` are fine; `userMinus` already in `SystemIcon` + `ChatTimeline` `SYS_ICONS`). Files: `lib/store.ts`, migration. (3) **Chat unread counts ONLY messages received from others** (your own sends never alert). `unreadCount` already excluded `sender_id === currentUserId` and the read marker (`threadReadAt`, client-only) survives live rehydrate (`fetchSnapshot` doesn't return it), so this was mostly already true; hardened by making **`sendMessage` advance your read marker** (`actions.markThreadRead(threadId)` at the top of both modes) so a message YOU send can never leave a stale "new messages" alert regardless of which screen is mounted. **Also removed the demo chat auto-reply** (`botReply`/`scheduleReply`/`CANNED_REPLIES`/`REPLY_DELAY`/`replyTick` deleted; `sendMessage` no longer schedules a canned response) so a self-send no longer triggers a phantom incoming message in mock mode — only real replies from others count. Invite auto-accept demo sim kept. File: `lib/store.ts`. (4) **Alerts badge clears the moment you open the Alerts tab.** `ChatListScreen` persists a per-user `chat:alertsSeen` count; opening Alerts marks the current `notifCount` seen and the badge only re-appears when a genuinely NEW alert arrives (seen is clamped to the live count so actioning/expiring an alert never resurfaces a stale badge). Badge condition is now `o.id==='alerts' && hasNewAlerts` (was `notifCount > 0`). File: `screens/chat/ChatListScreen.tsx`. `tsc -b` + eslint clean throughout._

_Earlier (2026-06-14) — **edit reflects on Home + invited-players list + MatchCard skill band.** Four user-requested polish items on the host/edit flow and the canonical card. (1) **`actions.updateMatch` now optimistically mutates the store in BOTH modes** (was live-only `afterWrite`), then live-mode persists + rehydrates to confirm — mirroring `cancelMatch` (§5). Edit → Save navigates straight to `/home`, which re-rendered from the not-yet-updated store, so a changed **start/end time** (and spots/price) kept showing the old value until the Supabase round-trip. Optimistic-first fixes it; the rehydrate returns the same persisted value so there's no flicker. File: `lib/store.ts`. (2) **Invited players are listed under the Invite button** on BOTH create/edit forms (`CreateMatchScreen` + `EditMatchScreen`): when join mode = Invite and players are picked, each renders as a row (accent monogram via `initials` + name + `skillLabel`, with an ✕ to remove); the button subtitle changed from a truncated comma list to "Tap to add or remove players." `CreateMatchScreen` (store flow) **preloads existing invites in edit mode** from the `matchInvites(db, matchId)` selector (excluding `declined`/`expired`/`left`) so re-opening an invite-only match shows who was already invited instead of an empty picker. **Stage-1 cut:** removing a row in the store-edit flow updates the form selection only — there's no revoke RPC yet (the create flow sends invites on initial create via `invitePlayer`; edit doesn't re-diff invites against Supabase). Files: `screens/matches/{CreateMatchScreen,EditMatchScreen}.tsx`. (3) **`MatchCard` shows the match skill as a BAND under the time**, on its own indented line with a `Gauge` icon in front — e.g. "Beginner → Low int.", "Low int. → High int.", "All levels". New `format.skillRangeLabel(skill_level)` expands each coarse `skill_level` back to a representative range (the store collapses the create form's min→max slider into ONE coarse value via `levelRange`, so the *exact* picked range isn't recoverable — the band is representative, not literal; persisting `skill_min`/`skill_max` is a deferred TODO if literal fidelity is needed). The time + level form one flex-col column; location stays the second column. (4) **Card time↔location gap widened** `gap-3.5` → `gap-6`. Files: `lib/format.ts`, `components/MatchCard.tsx`. `tsc -b` + eslint clean._

_Earlier (2026-06-14) — **chat room: one-renderer timeline + inline decision/invite cards + group chat.** Built the chat-room spec (§1/§5/§7/§8) fully on the live Supabase path. (1) **§1 one renderer, many message types** — new `lib/types.ts` `TimelineItem` union (`text · system · invite · decision`) + `store.threadTimeline(threadId)` selector that merges `chat_messages` with cards **DERIVED from the persisted `match_requests`** (no new storage — the cards render live, RLS-scoped, Realtime-synced DB state, per the read-time convention). New shared `screens/chat/ChatTimeline.tsx` is the single dispatcher (owns the scroll); `MatchChatScreen`, `ConversationScreen`, and the new `GroupChatScreen` all render through it, so bubble/line/card styling lives in ONE place (deleted the duplicated `SystemLine`/bubble JSX from the screens). (2) **§7 decision cards** — a host's inbound join requests appear inline in the match thread as Approve/Decline cards (visible because `mr_read` already lets the host see their match's requests); on approve the live `_sync_thread_on_join` trigger posts the "X joined" follow-up line, on decline the card collapses to a host-only "You declined …" follow-up (derived from the now-`declined` row, RLS-private so no leak). Mock `approveRequest` was upgraded to mirror the trigger (adds player to thread + posts "joined") and now sets `joined` (was `approved`) for live parity. (3) **§5 DM invite-to-play** — `ConversationScreen` ⋯ menu gains "Invite to play" → new `screens/chat/DmInvitePicker.tsx` lists the host's invitable matches (`store.invitableMatchesFor`) → `invite_player` RPC → the invite renders as a card in the DM timeline for BOTH sides (actionable Accept/Decline for the invited player, "Invite sent" for the host; resolves to a follow-up line). (4) **§8 group chat** — `NewMessageScreen` is now multi-select: 1 picked → canonical DM (`get_or_create_dm`), 2+ → a group thread via the **new `create_group_thread(uuid[])` RPC** (migration `20260614000002_connect_group_threads`, applied live + tested: 3-member group + opening system line, exact-set reuse never forks, single-other resolves to the DM). Group threads use the existing participant-based chat RLS + Realtime unchanged. New route `/chat/group/:threadId` → `GroupChatScreen`; `ChatListScreen` renders group rows (Users glyph + member count, links to the group) and its pinned card now prefers a **live** match (pulsing "Live" pill) over a merely-today one (§3). New store selectors: `threadById`, `isGroupThread`, `threadOthers`, `threadTitle`, `threadTimeline`, `invitableMatchesFor`; new action `createGroupThread`. (5) **Cancelled match → chat auto-removed from the inbox** — `inboxThreads` now drops any thread whose match is `cancelled`, so the conversation disappears from the Chat page for every participant on the next render (live: the status flip rides Realtime). The thread row is kept (not hard-deleted) so the read-only `MatchChatScreen` is still reachable by deep-link and history survives. (6) **Fourth Chat filter pill "Alerts" = the notifications section** — collects every review item in one place: **invitation review** (`myInvites`, player), **request review** (new `myHostRequests` — pending join requests on matches you host), and **waitlist review** (new `myWaitlistEntries` — matches you're queued on, with FIFO position + Leave). The pill carries a live count badge; selecting it shows only the three grouped review feeds (Invitations / Join requests / Waitlist) with an "All caught up" empty state, while **All** still surfaces them above the threads. Reviews no longer render on the Matches/People filters (they're threads-only). Request approve/decline here mirrors the inline §7 decision card; both write through the same `approveRequest`/`declineRequest` RPCs so the two surfaces stay consistent. Files: `screens/chat/ChatListScreen.tsx`, `lib/store.ts`. (7) **Archive pill + swipe-to-archive/delete** — a 5th Chat pill **Archive** (the pill track is now horizontally scrollable, `hscroll w-fit max-w-full`, so five fit any width). Each conversation row is wrapped in a new `screens/chat/SwipeRow.tsx` — swipe left to reveal **Archive · Delete** (in the Archive view the first button is **Unarchive**). **Input handling is cross-device:** touch + mouse press-drag via pointer events (axis-locked so vertical drags still scroll the list; native link/image dragging + text selection suppressed so the gesture isn't hijacked), **plus a laptop trackpad two-finger horizontal swipe** via a non-passive `wheel` listener (`preventDefault` stops the browser's back/forward swipe; momentum-aware snap on idle). A click while open / right after a swipe is swallowed so it doesn't navigate. The action buttons are **transparent — just the icon + word in the action's own colour** (Archive/Unarchive in `--color-accent`, Delete in `--color-danger`), not solid colour blocks. State is **device-local, persisted to `localStorage` `connect.chatPrefs`** (no Supabase table — same class as block/save/read): `db.archivedThreadIds` (set) + `db.deletedThreadAt` (map). `inboxThreads` now also excludes archived + deleted threads; new `archivedThreads` selector feeds the pill; `isThreadArchived`/`isThreadDeleted` helpers. **Delete is a soft per-device hide** (the Supabase thread is untouched so other participants keep it) and **resurfaces on a newer message or when you reopen it** (`markThreadRead` clears the delete stamp) — iMessage-style, no data loss. New actions `archiveThread`/`unarchiveThread`/`deleteThread`; `clearClientState` wipes `connect.chatPrefs` on sign-out. Files: `screens/chat/{SwipeRow,ChatListScreen}.tsx`, `lib/store.ts`. **Known Stage-1 cut:** host "waitlist" response to a request isn't built — requests expire when a match fills and players self-waitlist (no host_waitlist RPC), so the host decision card is Approve/Decline only. `tsc -b` + eslint + `vite build` clean. Files: migration `20260614000002_connect_group_threads.sql`, `lib/types.ts`, `lib/store.ts`, `screens/chat/{ChatTimeline,DmInvitePicker,GroupChatScreen}.tsx` (new), `screens/chat/{MatchChatScreen,ConversationScreen,NewMessageScreen,ChatListScreen}.tsx`, `App.tsx`. **Live test:** dave@connect.test already has a pending request on his approval match → its chat shows the decision card on login; two windows (alice + bob) exercises the full request→approve→"joined" Realtime round-trip._

_Earlier (2026-06-14) — **court "Court N" rendering + card location moved to the second line + host on every card + Settings back→Home.** (1) **Single court formatter `courtNumberLabel`** (`lib/format.ts`): renders a court value as "Court N", but **only prefixes "Court " when the stored value is a bare number** (`/^\d+$/`), so user-typed `"4"` → "Court 4" while seed values that already carry a word ("Court 4", "Hall 2", "Court A") pass through unchanged (no "Court Court 4"). `courtLabel` now goes through it; applied at every display site — `MatchCard` location line + title, `MatchDetailsScreen` ("{venue} _at_ Court N", was `· {court}`), `HomeScreen` Next-up data (was its own `` `Court ${…}` `` → would've doubled), `HostedMatchCard` (demo card, was hardcoded `· Court {court}`). `WeekMatchCard`/`ProfileScreen` inherit it via `courtLabel`. The **VenuePicker** court-number field's inline prefix label now reads **"Court"** (was "No."). (2) **MatchCard location moved to the body second line.** The uppercase venue/court overlay on the **top-left corner of the card art was removed**; the full `locationLine` (`venue · Court N · setting`) now renders on the second line next to the `MapPin` icon (previously that line showed only `venue_name.split(' ')[0]`). (3) **"Hosted by" on every card.** All `MatchCard` instances now pass `host`; the only gaps were `AllMatchesScreen`'s two cards (My Matches upcoming + past), now `host={getUser(db, m.host_id)}` (added `getUser` import). (4) **Settings `<` → `/home`** (`SettingsScreen`, was `navigate(-1)`): after a profile save→`/profile`→`/settings` hop, `navigate(-1)` bounced back to the profile page; now the loop completes avatar → Settings → View/Edit Profile → (save→View Profile) → back → Settings → back → Home (§6). Files: `lib/format.ts`, `components/MatchCard.tsx`, `screens/matches/{MatchDetailsScreen,AllMatchesScreen,VenuePicker}.tsx`, `screens/home/{HomeScreen,HostedMatchCard}.tsx`, `screens/profile/SettingsScreen.tsx`. `tsc -b` clean._

_Earlier (2026-06-14) — **Home no longer blanks when the profile row is missing.** A live account with no `public.users` row (e.g. created via a path that skipped/failed the `handle_new_user` trigger) made `FirstTimerHome`'s `getUser(db, currentUserId)!` undefined → `me.name` threw → **blank screen** (observed on `jian@connect.test`). Fix: `FirstTimerHome` reads `me` optionally and falls back for identity/prefs (`onboarding.name ?? me?.name ?? 'there'`, sport→`'padel'`, skill→`'any'`); `HomeScreen` drops the `!` on `getUser` and routes to `FirstTimerHome` when `!me` (a user with no row can't own matches anyway — `matches.host_id`/`match_players.player_id` are FK-bound to `users`). So the empty-state Home (Find a match / Host one + seeded rail) renders safely even pre-profile. **Data fix** for an orphaned auth user: backfill `public.users` from `auth.users` (insert id/name/email + NOT-NULL `gender` fallback + placeholder sport/skill/dob, `where not exists`); user repairs real values via Edit Profile. **Root-cause guard:** the `handle_new_user` trigger should always set `gender` (with a `coalesce(... ,'male')` fallback) so signups can't orphan when `users.gender` is NOT NULL — see §7. Files: `screens/home/FirstTimerHome.tsx`, `screens/home/HomeScreen.tsx`._

_Earlier (2026-06-14) — **Home split confirmed: empty-state vs returning-user.** Two distinct Home views, chosen purely by data (§4): a user with **no joined and no created match** (brand-new OR existing) gets **`FirstTimerHome`** — the **Find a match** + **Host one** buttons and the "Looking for players" seeded-feed rail. A user **with** any joined/hosted match (or pending invite) gets **`HomeScreen`** — Next up · You're hosting · This week · Saved, and **NO Find/Host buttons or discover rail** (just the small header **+** for hosting). The switch is `HomeScreen`'s guard `data.joined.length === 0 && data.hosted.length === 0 && !hosted && data.invited.length === 0 → <FirstTimerHome/>`. (Note: an earlier same-day edit that added the CTAs + rail to the returning-user `HomeScreen` was reverted — those belong only on `FirstTimerHome`.) File: `screens/home/HomeScreen.tsx`._

_Earlier (2026-06-14) — **host-create routing single-sourced across all entry points.** The Home header **+** (`HomeScreen`) and the My-Matches/All header **+** (`AllMatchesScreen`) were still hardcoded to `/matches/create`; both now use the mode-aware `HOST_CREATE_ROUTE` const, joining `FirstTimerHome` + `DiscoverScreen`. In live mode this resolves to `/matches/create` (no behaviour change), but routing is now genuinely single-sourced so hosting behaves identically for every account — the seeded/dummy users and any future signup — and can't drift per-screen. Note: with Supabase env present, `signInWithMock` is disabled (`AuthContext`) and every account runs the live store flow, so the routing already applied uniformly; this removes the last hardcoded paths. Files: `screens/home/HomeScreen.tsx`, `screens/matches/AllMatchesScreen.tsx` (§7)._

_Earlier (2026-06-14) — **create-form spots/host/time/price polish + cancel & edit count fixes.** (1) **Cancel now reflects on Home.** Live `actions.cancelMatch` only fired an async `afterWrite` and returned, so `CreateMatchScreen`'s immediate `navigate('/home')` re-rendered before the round-trip and the match lingered under "You're hosting". Now `cancelMatch` optimistically mutates the store (status `cancelled` + waitlist expire) in **both** modes, then live-mode `afterWrite`s + rehydrates (§5). (2) **Spots = "players needed".** The Players slider now picks how many **others** the host needs (1–7, "Players needed · Not counting you"); `total_spots = needed + 1`, so a card reads "{needed} spots left" right after create — e.g. need 1 → "1 spot left" (singles). Fee total + avatar dots use the full roster (`needed+1`). **Edit recomputes `spots_available`** = `max(newTotal − joined, 0)` so changing the count can't drift the card (was a real bug: `total_spots` updated, `spots_available` didn't). Files: `screens/matches/CreateMatchScreen.tsx`, `screens/matches/EditMatchScreen.tsx` (§4/§5). (3) **`MatchCard` always shows the host.** Home "You're hosting" card now passes `host={getUser(db, m.host_id)}` (was omitted → no "Hosted by" line); the avatar stack prepends the host to its `roster` if missing from `players`, so the host monogram heads the stack even pre-hydration. Files: `screens/home/HomeScreen.tsx`, `components/MatchCard.tsx`. (4) **Time field = HH `:` MM.** `WhenCard.TimeField` is now two digit-only cells with a fixed `:` between them (was one free `HH:MM` text field), still committing via `normalizeTime`. (5) **Price typing-only.** Both forms' price inputs are `type="text"` + `inputMode="numeric"` + digit sanitise + `.num` (no steppers; was `type="number"`). All four user-requested. Verified `tsc -b` + lint clean on touched files._

_Earlier (2026-06-14) — **Home "You're hosting" groups all hosted matches under one header.** Previously `HomeScreen` mapped each hosted match into its own block, so a user hosting several matches saw a repeated "You're hosting · {date}" eyebrow + per-card "See all". Now (live/seeded store branch) there is a **single** "You're hosting" header + one "See all" link, with every hosted match (open/full, from `hostedMatches(db)`) stacked under it in a `flex-col gap-3.5` (same pattern as "This week"); the dropped per-card date label removed the now-unused `dayLabel` import. The mock-mode localStorage `HostedMatchCard` branch (single `connect:hostedMatch`) is unchanged. File: `screens/home/HomeScreen.tsx`._

_Earlier (2026-06-14) — **match name persisted + shown on the card.** A live-created match's typed **name** never appeared on the Home card (and wasn't persisted): the `matches` table had no `name` column, `create_match` didn't take one, and `MatchCard`'s title is `matchKind at courtLabel` (so it showed e.g. "Doubles at 4"). Fix: migration `add_matches_name_and_create_match_name` adds `matches.name` (text, nullable) and recreates `create_match` with a `p_name` param (dropped/recreated since the signature changed); `store.createMatch` passes `p_name`, `store.updateMatch` adds `name` to its editable cols, `repo.mapMatch` reads it, and `MatchCard` renders `match.name` as the title when present (else the derived label). Location was already persisting (`venue_name`/`venue_location`/`court_number` via the RPC + `mapMatch`) and shows on the card art + MapPin line — verified the user's rows had correct venue data; the perceived "location wrong" was the court-number-only title, now replaced by the name. Verified end-to-end (RPC create with `p_name` → row has name+venue; test row cleaned up). Files: `lib/store.ts`, `lib/repo.ts`, `components/MatchCard.tsx` + Supabase migration. NB: matches created before this (e.g. the user's two 12:18 rows) have `name=null` and show the derived title — re-create to set a name. Files (earlier same-day entries below)._

_Earlier (2026-06-14) — **host-create routing fix (live).** A match created by a (live) user wasn't showing under Home's "You're hosting" because the host buttons routed to the **demo** flow `/matches/create-demo` (`EditMatchScreen` → `writeHostedMatch` localStorage only), and live-mode Home **ignores** `connect:hostedMatch` (browser-global key → cross-account bleed), relying solely on `hostedMatches(db)` from Supabase — so the match never persisted or displayed (confirmed: 0 rows in `public.matches` for the new user). Fix: a single mode-aware **`HOST_CREATE_ROUTE`** const in `lib/hostedMatch.ts` (live → `/matches/create` store flow / `actions.createMatch` RPC → persists + rehydrates → shows; mock → `/matches/create-demo`), wired into all host entry points (`FirstTimerHome` "Host one", `DiscoverScreen` header **+** and footer nudge). Files: `lib/hostedMatch.ts`, `screens/home/FirstTimerHome.tsx`, `screens/discover/DiscoverScreen.tsx`. Existing demo-created matches stay localStorage-only (never reached the DB) — re-create via the fixed button to persist._

_Earlier (2026-06-14) — **live sign-up + Settings navigation.** (1) **Sign-up now calls real `supabase.auth.signUp`** on the final "Creating account" step (`CreatingAccountScreen`), fixing the bug where signup never created an auth user (the flow only ever called `signInWithPassword`). New `signUp(email,password,{name,gender})` on `AuthContext` → `{error,session,needsConfirmation}`; email/password carried across the questionnaire via new `onboarding.email`/`onboarding.password` draft fields (password cleared on success); after a session it writes `{dob,sport,skill_level,gender}` to the `users` row (authenticated, RLS `id=auth.uid()`), then re-`connectLive`s from a post-write snapshot so Profile/Home reflect the captured sport/skill immediately. Error → error screen; no session (email-confirmation ON) → "Check your email"; never silently routes to `/home`. The `handle_new_user` trigger seeds name/gender from `options.data` (real gender, not the `'male'` stub). Verified end-to-end against the live project; **email-confirmation must be OFF** for the Stage-1 flow to reach `/home` (§7). (2) **Settings navigation:** both Home variants' top-right avatars now link to `/settings` (was `/profile` on `FirstTimerHome`); the `<` back button on the own View Profile + Edit Profile routes to `/settings` (other players' `/players/:id` keeps `navigate(-1)`) — loop is avatar → Settings → View/Edit Profile → back → Settings (§6). Files: `screens/auth/AuthScreens.tsx`, `context/AuthContext.tsx`, `lib/onboarding.ts`, `screens/home/FirstTimerHome.tsx`, `screens/profile/ProfileScreen.tsx`, `screens/profile/EditProfileScreen.tsx`._

_Earlier (2026-06-13) — **sport emoji + profile edit/carry-forward + live store + sign-out.** (1) **Sport glyph = emoji** (`lib/sports.tsx` `SPORT_EMOJI`/`sportEmoji`): 🏓 padel · 🎾 tennis · 🏸 badminton · 🏃 running, shown in front of the label on the create/edit sport pills, the profile Sports & level rows, and `SportsLevelSection` — replaced the old lucide marks; add new sports to the one map (§3). (2) **Home "Next up" countdown** (`format.countdownUntil`): days when ≥24h out (`"3d"`), hours under a day (`"5h"`), `"soon"` in the final hour. (3) **Edit Profile**: save-then-routes to **`/profile`** (View Profile, not Settings) + 2s "Profile updated" toast; **carry-forward** seeds the sports list from `readProfileSports()` so every saved sport+level rehydrates (never blank/default); name split into First/Last fields; **city + area are now `<select>`s** sourced from `lib/locations.ts` (`QATAR_CITIES`/`areasFor`), and `updateProfile` persists `city` too. (4) **ProfileScreen**: Settings gear removed (own header is **Edit-only** — standing rule, §6); **first** name highlighted in italic accent (was surname); court `SportArt` thumbnail removed from the Sports & level card; that card is now **multi-row** — canonical primary sport+level from the store `users` row (so it can't drift from `/players/:id`, cards, roster, chat — §5) plus, on the **own** profile only, the extra sports from `readProfileSports` (others see only the primary). (5) **Store/live**: `updateProfile` writes `name/bio/area/city/sport/skill_level` to the `users` row + optimistic mutate; new **`clearClientState()`** used by **SettingsScreen**'s real sign-out (`useAuth().signOut()` → on success `clearClientState()` + `restoreDemoAccount()` → `/login`, `signingOut` guard, no half-sign-out). The dual-mode store's **live Supabase path is wired** (`connectLive`/Realtime `connect-db` channel/`rpc`/`afterWrite`) and `.env.local` is present, so the running app is live (§7). (6) **Types/seed**: `User` gains `city`/`verified`; `repo.mapUser` reads `languages/bio/area/city/verified`; mock users seeded `city:'Doha'`/`verified:true` + a new invite-only tennis thread. (7) **Invite flow**: Chat invitation card now has **inline Accept/Decline**, and `acceptInvite`'s `'joined'|'expired'` return drives race-safe routing into `/chat/match/:id` (Chat, Match Details, Home). Earlier: profile layout + full field-sync: the **ProfileScreen** hero now stacks identity as **name → location → bio** directly under the name (location = muted line with a non-flipped `MapPin`, §7; empty location hides; empty bio shows a subtle "Add a bio" deep-link on the **own** profile only, hidden on `/players/:id`); the separate lower Location card was removed. `actions.updateProfile` persists `name/bio/area/sport/skill_level` — the live `users` table gained **`bio` / `area`** columns (plus a `languages text[]` column, read-only for now — display only, no editor; migration `add_profile_bio_area_languages`; RLS already public-SELECT + self-UPDATE so other players read them and Realtime propagates), and `repo.mapUser` reads them back. Single source of truth unchanged: every surface (View Profile, `/players/:id`, Settings header, `MatchCard`/Match Details roster/chat identity) reads the one `users` row from the store, so sport/skill/name/avatar-initials/location/bio reflect everywhere with no manual refresh. Earlier: invite-only join path wired end-to-end: `createMatch` now auto-creates the per-match group chat thread with the **host as sole member** (§5 — no chat before joining), and `actions.acceptInvite` adds the accepting player to that thread + posts a "[Name] joined" system line + returns `'joined' | 'expired'` so callers route into `/chat/match/:id` on success or toast "Match just filled" on a race loss (Home invite sheet, Chat invitation card, Match Details all use this). Pending invites still hold no slot (`myInvites` filters to joinable matches at read time → auto-expire); "Decide later" leaves the invite `invited` and it surfaces as an **inline Accept/Decline card in Chat** (no Home bell, §4), with the Home pop-up gated once-per-invite via `connect:invitesSeen`. Note: strings are hardcoded EN with RTL-safe markup (logical props, `.ltr-nums`, `rtl:rotate-180`) — the app has no active i18n runtime, so no new i18n keys were added (consistent with the codebase). Earlier: Supabase Auth wired (`lib/supabase.ts` shared client + `context/AuthContext.tsx` `getSession`/`onAuthStateChange`, `useAuth()` exposing `signIn`/`signInWithMock`/`signOut`; dormant until `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` are set, dev-`mockUser` fallback otherwise — §7); onboarding identity on every screen (`connect.onboarding` name/sport/skill overlaid via `withSignedIn`/`freshDB` so sign-up name + avatar initials win over the dev `mockUser` and survive reload), fresh-account clean slate (`startFreshAccount` clears `connect:hostedMatch`), the shared+filtered first-timer Home feed, the localStorage hosted-match flow, shared `mode` create/edit form, the shared `WhenCard` + `lib/datetime` date/time UI (typeable 24h times, no AM/PM, native calendar date picker), numbers-only + scroll conventions._
